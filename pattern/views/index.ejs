<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>List of links (total = <%= links.length %>):</p>
    <ol>
      <% links.forEach(link => { %>
        <li><a href="<%= link %>" target="_blank"><%= link %></a></li>
      <% }); %>
    </ol>
  </body>
</html>
